#include "effect.h"

#include "../game_struct.h"
#include "../maps/cell.h"
#include "../maps/map.h"

void VisualEffect::showEfect() {
  //....
  // цикл
  // {
  //  gameStruct.map->getCell({x, y})->setEffect(true, effectIdentifier);
  //  }
  // gameStruct.map->emit();
  // std::thread::this_thread::sleep(0.3);
  // цикл
  // {
  //  gameStruct.map->getCell({x, y})->setEffect(false, effectIdentifier);
  //  }
  // ...
}
